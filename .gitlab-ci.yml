variables:
  USER_NAME: $USER_NAME
  USER_TOKEN: $USER_TOKEN

stages:
  - build

build:
  stage: build
  image: antora/antora:latest
  script:
    - antora antora-playbook.yml --fetch
  artifacts:
    paths:
      - ./public

